services:
  backend:
    build:
      context: ./backend
    environment:
      FLASK_ENV: production
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./frontend
    depends_on:
      - backend
    ports:
      - "4200:4200"
